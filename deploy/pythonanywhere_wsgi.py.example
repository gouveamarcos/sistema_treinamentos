import os
import sys

project_path = "/home/marcosgouvea/sistema_treinamentos"
if project_path not in sys.path:
    sys.path.insert(0, project_path)

os.environ["DJANGO_SETTINGS_MODULE"] = "treinamentos.settings"
os.environ["DJANGO_DEBUG"] = "False"
os.environ["DJANGO_SECRET_KEY"] = "COLOQUE_AQUI_UMA_CHAVE_LONGA_E_SECRETA"
os.environ["DJANGO_ALLOWED_HOSTS"] = "marcosgouvea.pythonanywhere.com"
os.environ["DJANGO_CSRF_TRUSTED_ORIGINS"] = (
    "https://marcosgouvea.pythonanywhere.com"
)

# Gmail. Use uma senha de app de 16 caracteres, não a senha normal da conta.
os.environ["EMAIL_BACKEND"] = "django.core.mail.backends.smtp.EmailBackend"
os.environ["EMAIL_HOST"] = "smtp.gmail.com"
os.environ["EMAIL_PORT"] = "587"
os.environ["EMAIL_HOST_USER"] = "gouvea.marcos@gmail.com"
os.environ["EMAIL_HOST_PASSWORD"] = "COLOQUE_AQUI_A_SENHA_DE_APP_DO_GOOGLE"
os.environ["EMAIL_USE_TLS"] = "True"
os.environ["DEFAULT_FROM_EMAIL"] = (
    "Academia Técnica <gouvea.marcos@gmail.com>"
)

from django.core.wsgi import get_wsgi_application

application = get_wsgi_application()
